public enum ItemType {
    Item, 
    Weapon
}

public enum Unit {
    Sum,
    Percentage
}

public enum CollectibleType {
    XP,
    Coin,
    Recovery
}

public enum GameState {
    Gameplay,
    Upgrade
}